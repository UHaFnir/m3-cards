{
  "name": "m3-cards",
  "version": "2.4.0-uhafnir.1",
  "description": "Material 3 style Lovelace cards for Home Assistant — 40 cards across energy, climate, light and media, household and safety — built natively with Lit, with no button-card or card-mod dependency.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "build": "vite build",
    "docs": "node scripts/build-readme.mjs",
    "deploy": "npm run build && bash scripts/deploy.sh",
    "check": "tsc --noEmit && eslint src",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:contrast": "esbuild src/shared/contrast.ts --format=esm --outfile=.contrast.mjs --log-level=error && node test/contrast.test.mjs $PWD/.contrast.mjs; rm -f .contrast.mjs"
  },
  "keywords": [
    "home-assistant",
    "lovelace",
    "custom-card",
    "hacs",
    "climate"
  ],
  "license": "MIT",
  "author": "UHaFnir",
  "repository": {
    "type": "git",
    "url": "https://github.com/UHaFnir/m3-cards.git"
  },
  "homepage": "https://github.com/UHaFnir/m3-cards",
  "bugs": "https://github.com/UHaFnir/m3-cards/issues",
  "dependencies": {
    "lit": "^3.2.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "eslint": "^9.15.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.15.0",
    "vite": "^5.4.21",
    "vitest": "^3.0.0"
  }
}
